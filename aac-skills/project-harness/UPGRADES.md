# Harness upgrade table

One row per harness version: what it added, and which step of `SKILL.md` installs it. Read the
rows above the repo's marker number and re-run only those steps.

**Two branches that both took the same number:** the branch that merges second keeps its row
text and takes the next free number — run `node tools/renumber-harness-upgrade.js` in
claude-dotfiles, which moves the number in every other place the branch wrote it and rebuilds
the generated bootstrap template. The fleet's deliver stage runs it for you (issue 515); never
renumber by hand, and never drop a row to avoid the collision.

| Version | Date | What it added | Re-run |
|---|---|---|---|
| 1 | 2026-07-28 | Triage labels, issue forms, `scripts/build-dashboard.js` + `dashboard.yml`, pre-commit test gate, ADR status lines, `docs/agents/` tracker + triage docs, Projects board | steps 1–7 |
| 2 | 2026-07-29 | `tools/tracker-audit.js` (live tracker drift audit) and this version marker | steps 8–9 |
| 3 | 2026-07-29 | `tracker-audit.js` gains Projects-board checks (`board-says-done`, `not-on-board`), PR-aware reference checking, and two false-positive fixes. **Same step, newer file** — a v2 repo has the tool but a stale copy | step 8 (re-copy the template, overwriting) |
| 4 | 2026-07-29 | `build-dashboard.js` gains the **From PRD** column (decomposition seen from the child's side) and a header warning not to commit locally regenerated `DASHBOARD.md`. **Same step, newer file** | step 3 (re-copy `build-dashboard.js`, re-apply the repo's `CONFIG`) |
| 5 | 2026-07-29 | Projects auto-add guidance rewritten from three verified findings (linking ≠ auto-add; enabling is not one click; the setting cannot be read, only tested) and the sweep made part of any template change. **Docs only — no file to re-copy**, but the owner action is real: enable Auto-add on each board and verify by filing an issue | step 5 (hand the owner the toggle; no repo files change) |
| 6 | 2026-07-30 | `dashboard.yml` stages with `git add -N` before diffing. Without it `git diff --quiet -- DASHBOARD.md` is blind to an **untracked** file, so on a repo with no dashboard yet the workflow printed "Dashboard unchanged", exited 0, and never created the artifact — a green run with nothing to show for it. Every fresh install was affected. **Patch, do NOT re-copy** | insert the two lines by hand — `dashboard.yml` carries per-repo test-environment setup (task-management's `setup-python` + `pip install -e ".[dev,classify]"`, message-board and aac-cockpit's `setup-node`), and a wholesale copy destroys it and reverts the default-branch substitution. Same hazard as step 3.3's `CONFIG` block |
| 7 | 2026-08-01 | Session checks: `.claude/session.json` + `docs/runbooks/session.md`, and `tools/clasp-auth.js` on clasp repos. The credential gate is the load-bearing part — it verifies the grant's SCOPES, not just that it refreshes, and `~/.clasprc.json` is shared machine-wide, so a bare `clasp login` in one repo silently narrows what every other one can do. Two of four clasp repos had no such gate | steps 12–13 (preserve any existing `.claude/session.json`) |
| 8 | 2026-08-02 | `.caveman.json` at the repo root, pinning session output intensity to `ultra` (Dan, 2026-08-02). Without a repo file the level comes from a user config that exists on one machine and in no history, so the same repo reads `full` on another checkout and nothing says why | step 14 (add the file only where it is missing; keep an existing value) |
| 9 | 2026-08-19 | `.claude/workflows/ticket-fleet.js` — named script for the in-session Workflow tool: parallel unblocked-ticket runs, pinned implementer model (full IDs verified working in `opts.model`, e.g. `claude-opus-4-7`), blind refuting verifier per attempt, PR only on verified pass, single-writer discovery collection. Also fixes `templates/harness-version.md`, which still said 7 after the v8 bump | step 15 (copy only where the file is missing) |
| 10 | 2026-08-21 | `build-dashboard.js`'s jest/pytest-shape summary branch now reports skipped and xfailed counts alongside pass/fail. Without it a platform-conditional test skipping in CI shrinks the pass count with no explanation (aac-contract-builder: 316 local vs 315 CI, a Windows-only test skipping on Linux) and the drift reads as healthy. **Same step, newer file** | step 3 (re-copy `build-dashboard.js`, re-apply the repo's `CONFIG`) |
| 11 | 2026-08-21 | `tracker-audit.js` gains two checks born from the aac-contract-builder mess post-mortem (strategies landed hours after the drift formed and could not have caught it anyway): **`unmilestoned`** (hard) — once any issue carries a milestone, an open issue outside every milestone is invisible to planning; inferred-from-data like the board check, silent NOTE when the repo uses no milestones. **`possibly-delivered?`** (advisory) — an open issue whose number appears as `issue N` in a commit subject on the current branch was probably delivered by a merge that lacked a `Closes #N` keyword; four issues sat open a day this way. The closing-keyword convention itself needs no new file: `ticket-fleet.js` already mandates `Closes #N` in PR bodies (template line ~113), and the advisory catches legacy/manual merges. Companion edit outside this skill: wayfinder's SKILL.md now tells charting to label tickets `needs-triage` at birth — wayfinder-created issues bypassed the issue-form intake guarantee. **Same step, newer file** | step 8 (re-copy `tracker-audit.js`, no substitutions) |
| 12 | 2026-08-21 | `tracker-audit.js` harmonized: the union of every improvement the five hand-extended repo copies had proven locally, so the template stops being the weakest copy on the machine. From **aac-cockpit**: the `landed-but-open` / `landed-but-open?` pair REPLACES v11's `possibly-delivered?` (reads whole commit messages via `git log --format=%h%x00%s%x00%B%x1e` spawned through `execFileSync` — cmd.exe eats `%` through a shell; closing keywords from the whole message are HARD, bare subject `#N` advisory; scans `origin/<default-branch>` after a bounded fetch, never HEAD); and the `proseBlockers` section-end fix — the template's `\Z` was a LIVE BUG (JS has no `\Z`; the ungated-dependency check silently missed every ticket whose `## Blocked by` was the last section — on adoption it immediately exposed a real missing edge on aac-contract-builder #60); and the `wontfix` skip on closed-with-open-boxes. From **aac-bill-intake**: `blocker-may-be-answered` (hard; an issue claiming to be blocked on a PR whose thread has newer comments than its `<!-- blocker-verified: #N @date -->` marker), PR fetch now carries comments, and `gh pr list` failure degrades dangling-reference to advisory + exits 2, never 0. From **aac-routines**: `isFollowUpAcknowledgment` gating on stale-premise? (a "Follow-up to closed #N" line or a closer-PR reference silences the whole body — kills the follow-up-ticket noise class) and `NOT_PLANNED_PATTERNS` filtering in untickedBoxes (a `superseded / not built / out of scope` bullet is a legitimate closure, not drift). The pure predicates export under a `require.main` guard so tests can pin them without shelling to `gh`; cockpit's 35-assertion parse test passes against the template verbatim. Issue fetch adds `closedByPullRequestsReferences`. Repo-specific checks stay local: cockpit's `unowned`/`conflicting-owner`, zoho's `milestone-boxes`/`milestone-doc-*`, routines' lib-file split, bill-intake's `updatedAt`. **Same step, newer file** | step 8 (re-copy `tracker-audit.js` on template-identical repos; port the universal patch set surgically on repos with local extensions — pull first, diff first) |
| 13 | 2026-08-21 | `ticket-fleet.js` pins a model per stage instead of implementer-only (Dan, 2026-08-21): `scoutModel: 'claude-sonnet-5'` (structured extraction), `implModel: 'claude-opus-4-7'` (unchanged — anecdotal reports have Opus 4.7 beating Opus 5 on coding, and the pin is version-stable), `verifyModel: 'claude-sonnet-5'` (skepticism comes from blindness + prompt, not tier), `deliverModel`/`reportModel: 'claude-haiku-4-5-20251001'` (pure mechanics). Orchestrator stays the main session's model. Rationale: frontier only where errors compound; cheap tiers where the task is bounded and checkable (Anthropic multiagent research, 15x-token cost shape). All five remain `args`-overridable | step 15 (re-copy on template-identical repos — CRLF-only drift counts as identical; on repos with local extensions, e.g. aac-routines' auth/cleanup phases and aac-cockpit's `PROMPT_CONTRACT`, port the cfg block + four `model:` opts surgically and run any local contract tests) |
| 14 | 2026-08-26 | Kill the nightly dashboard cron by making DASHBOARD.md drift-free (claude-dotfiles issue 21). `build-dashboard.js` swaps the relative-age `ago()` helper for `fmtDate()`, which returns `iso.slice(0,10)` — an ISO calendar date instead of "today" / "N days ago" — in the Updated column and every attention-list mention. Same input, same output; a rerun with no other content movement produces byte-identical bytes. `dashboard.yml` drops the `schedule: - cron: '0 11 * * *'` block: the only thing the daily tick did was bump those relative labels, so with the labels stable the tick has nothing to do, and the "chore: refresh dashboard [skip ci]" no-op commits it produced go away with it. Push and issue events still trigger, and the `git add -N` + `git diff --quiet` guard still ensures a commit lands only when content actually changed (v6 protection). **Both files change, patch surgically** — repo-local `CONFIG` blocks and per-repo test-environment setup make a wholesale re-copy destructive (same hazard as v3.3's CONFIG splice and v6's dashboard.yml patch note) **Cron footnote (2026-09-09):** drop the `schedule:` block only where the workflow also listens to `issues:` events. A repo that replaced issue events with a cron (the 2026-08-10 Actions-quota ruling, recorded in that repo's `dashboard.yml` header) keeps it: there the cron is the tracker-refresh path, not a label-bumper, and with `fmtDate` it commits only when tracker content moved. Which repos that is, read from each `dashboard.yml`, never from here | step 3 (patch `scripts/build-dashboard.js`: replace the `ago` function definition with `fmtDate` and swap the four call sites) + step 4 (patch `.github/workflows/dashboard.yml`: delete the `schedule:` block and its `- cron:` line) |
| 15 | 2026-08-27 | Branch-split the dashboard artifact (claude-dotfiles issue 27, ported from issue 20). `dashboard.yml`'s final `git push` becomes `git push -f origin HEAD:refs/heads/dashboard`: the commit-if-changed step still commits `DASHBOARD.md` with `[skip ci]`, but the push force-lands on a dedicated `dashboard` branch instead of advancing the default branch. Without this, every push/issue event that mutated the dashboard advanced origin on a file that has no bearing on source state, and on claude-dotfiles that flipped drifted live copies into state3 (both-diverged hard block) — the single largest source of session-start hard blocks between 2026-08-25 and 2026-08-26. Stable read URL: `https://github.com/<owner>/<repo>/blob/dashboard/DASHBOARD.md`. Affected repos on this machine (5): aac-sales-commissions, aac-sales-cockpit, aac-task-management, aac-bill-intake, message-board. **`dashboard.yml` change is small enough to patch surgically** — repo-local test-environment setup and default-branch substitutions make a wholesale re-copy destructive (same hazard as v6's dashboard.yml patch note and v14's above); swap only the final `git push` line for the branch-split block and leave the rest of the file alone | step 4 (patch `.github/workflows/dashboard.yml`: replace the final `git push` with `git push -f origin HEAD:refs/heads/dashboard`; verify no `refresh dashboard` commit lands on the default branch after the first CI run) |
| 16 | 2026-08-27 | Finish the drift-free artifact by dropping the generation-time header (claude-dotfiles issue 26). v14 stabilized the body but the header line still read `_Generated <iso> at commit <sha> ...`, so two back-to-back runs of `build-dashboard.js` on unchanged repo + tracker state produced two different files: the timestamp always moved, and the sha moved whenever HEAD did for unrelated reasons. `build-dashboard.js` now emits `_Generated by scripts/build-dashboard.js (CI: dashboard.yml). Do not edit by hand._` — no timestamp, no sha — and the now-unused `sha` constant is deleted. Same input, same output, byte-identical, so the `git diff --quiet` guard fires only on real content movement. **Same step, patch surgically** — the fix is one call-site and one deleted `const`; keep the repo-local `CONFIG` block intact | step 3 (patch `scripts/build-dashboard.js`: delete the `const sha = ...` line and swap the header line to the timestamp-free form) |
| 17 | 2026-09-09 | `.claude/settings.json` gains `extraKnownMarketplaces` + `enabledPlugins`, so a cloud session (claude.ai/code) installs the `aac-skills` plugin at startup. Without it a cloud session has none of these skills: the claude.ai account-level plugin sync returns zero plugins for the account (`plugins_sync_no_changes count:0` in the session diag log), and the cloud environment setup script runs before the session's git credentials exist, so `claude plugin marketplace add` fails there on the private clone. Verified in a cloud container (claude-dotfiles#100): a fresh startup with only these two keys cloned the marketplace and loaded all 56 skills plus the plugin's SessionStart hook. **Merge, never overwrite** — repo copies carry `permissions` (aac-bill-intake, aac-contract-builder) and `hooks` (aac-sales-cockpit, claude-dotfiles); `templates/add-cloud-plugin.js` adds the two keys and leaves the rest. **Sweep already performed 2026-09-09** on all eight active repos (claude-dotfiles#100, aac-bill-intake#583, aac-sales-commissions#45, aac-routines#154, zoho-source-of-truth#81, aac-contract-builder#189, aac-message-board#15, aac-sales-cockpit#593); only the marker bumps remain | step 16 (`node templates/add-cloud-plugin.js <repo>` where the keys are missing — a no-op on the eight above; bump the marker) |
| 18 | 2026-09-14 | `ticket-fleet` becomes a team skill served by the `aac-skills` plugin (`aac-skills/ticket-fleet/ticket-fleet.js` in `claude-dotfiles`), and the harness stops copying its own `.claude/workflows/ticket-fleet.js`. One script now serves local and cloud sessions - it picks between the `gh` CLI and the GitHub MCP tools at run time (`CLAUDE_CODE_REMOTE_SESSION_ID` set, or no `gh` on PATH). The three drifted copies before v18 (`.claude/workflows/ticket-fleet.js` in `claude-dotfiles`, `orchestrator/ticket-fleet-cloud.js`, and `aac-skills/project-harness/templates/ticket-fleet.js`) are removed. Together the merged script now carries `runId` from args, `defaultBranch`, `keepOpen` (Refs vs Closes in the PR body), and the MCP/gh instrument switch — none of which the harness template ever had at once. **The plugin pointer is step 16; step 15 is now "the fleet is served by the plugin"** — a repo needing a forked script keeps its own `.claude/workflows/ticket-fleet.js` and invokes it by name (aac-routines' auth/cleanup phases and aac-cockpit's `PROMPT_CONTRACT` are the two known forks) | step 15 (delete the repo's `.claude/workflows/ticket-fleet.js` UNLESS it carries local extensions; step 16 puts the plugin pointer in `.claude/settings.json` so `scriptPath = ${CLAUDE_PLUGIN_ROOT}/skills/ticket-fleet/ticket-fleet.js` resolves) |
| 19 | 2026-09-15 | Widened `autoMode.allow` ruling (Dan, 2026-09-15, issue 245) reaches every harnessed repo via step 16. `templates/claude-settings.json` gains `permissions.defaultMode: auto` + a blanket allow list (`Bash(*)`, `Edit`, `Write`, `mcp__github__*`) and a single `autoMode.allow` prose entry that sanctions **every action an unattended session takes, destructive and irreversible included** — across every classifier category (Self-Modification, Data Exfiltration, Exfil Scouting, External System Writes, Destructive Operations, Irreversible Operations, Credential Handling and any successor). `templates/add-cloud-plugin.js` now merges four keys instead of two: it fills `permissions.defaultMode` only if absent (never downgrades an existing `bypassPermissions` / `acceptEdits`), unions the allow list, and prepends the ruling into `autoMode.allow` if not present. Idempotent verified against empty and partial fixtures. Supersedes the 2026-09-14 acceptEdits ruling (`docs/cloud-permission-posture-2026-09-14.md`) and the narrow prose rules that followed it; the 2026-09-14 doc is retained marked-superseded and the successor lives at `docs/cloud-permission-posture-2026-09-15.md`. **Delivered by issue #218 across every harnessed repo** | step 16 (`node templates/add-cloud-plugin.js <repo>` on every harnessed repo — idempotent no-op on the eight already at v17/v18 for the marketplace keys but WILL add the widened rules; bump the marker) |
| 20 | 2026-09-16 | `tracker-audit.js` is no longer a hand-maintained second copy (claude-dotfiles issue 336). It is GENERATED from `claude-dotfiles`'s own `tools/tracker-audit.js` by `tools/build-harness-tracker-audit.js` — source plus one banner, no allowlist of "deliberate" differences, because an allowlist is what made the previous drift unreadable (an agent could not tell a ported divergence from a forgotten one). `tools/tracker-audit-template.test.js` fails the moment the two disagree, so the template can no longer fall behind unnoticed. The template gains the two fixes it had already missed: the **citation narrowing** (`citedIssueNumbers` — no word character or `/` before the `#`, none after the digits, so `owner/other-repo#157` and the hex colour `#9a690f` stop reading as this repo's #157 and #9; 16 of 29 `stale-premise?` advisories on claude-dotfiles were that false positive) and the advisory **`duplicate-title?`** check (two open issues whose titles match once stop words and qualifiers — still, again, the — are stripped; the shape of #281 and #285, one finding filed twice two minutes apart by two discovery-triage chores in one fleet wave). **Same step, newer file** | step 8 (re-copy `tracker-audit.js`, no substitutions — on a repo with local extensions to its audit, diff first and hand-apply the two checks, same rule as v12) |
| 21 | 2026-09-16 | `tracker-audit.js` gains **`deleted-subject?`** (advisory, claude-dotfiles issue 361): an OPEN issue whose unticked acceptance box names a repo path the default branch no longer has, where `git log --diff-filter=D` shows that path deleted. The neighbouring `stale-premise?` reads issue-to-issue citations, so it can only see a ticket whose premise another ticket wrote about — claude-dotfiles #120's `writing` box outlived `aac-skills/writing/` by a month (deleted by issue 178, which #120 predates and neither cites), and two fleet attempts spent a cycle discovering the box could not be ticked by doing the work. Narrowed to DELETED rather than merely absent, because a box naming a path the ticket will create is the normal case; a bare name is only read as a path inside backticks, matched against a nested directory git deleted, and never when the live tree still uses the name anywhere. The four pure helpers (`boxPathCandidates`, `deletedPathIndex`, `matchDeletedPath`, `deletedSubjectFindings`) export under the `require.main` guard, so a repo can pin them without shelling to `gh`. **Same step, newer file** | step 8 (re-copy `tracker-audit.js` on template-identical repos; on repos with local extensions port the four helpers, check 10, the `ORDER` entry, the NOTE and the `blind` term surgically — diff first) |
| 22 | 2026-09-16 | `tracker-audit.js` narrows two advisories that fired on meta-tickets by design (claude-dotfiles issue 374). `stale-premise?` no longer reports a closed-issue citation whose surrounding prose presents it as an EXAMPLE (`EXAMPLE_CITATION_PATTERNS` — example, e.g., such as, duplicate, dedupe, cautionary, precedent, illustrates), and a body can opt out outright with `<!-- tracker-audit-ignore: stale-premise -->`, or per citation with `... stale-premise #281`. Every discovery-triage chore that warns the next agent about a settled duplicate used to trip it, so the chore template and the advisory were in permanent tension. `deleted-subject?` skips an acceptance box that cites another ISSUE of the tracker — the box quotes that issue's box, so the deleted subject is that ticket's, not this one's; a cited PR number is not an issue number, so a box telling the owner to copy a path a PR deleted still reports. Measured on claude-dotfiles: `stale-premise?` 4 findings -> 3 (#358's example gone, three genuine kept), `deleted-subject?` 3 -> 2 (#361's quotation gone, #120 and #173 kept). `stalePremiseFindings` is now a pure export alongside `isExampleCitation`, `stalePremiseIgnores` and `boxQuotesAnotherIssue`. **Same step, newer file** | step 8 (re-copy `tracker-audit.js` on template-identical repos; on repos with local extensions port the three helpers and their four call sites surgically — diff first) |
| 23 | 2026-09-16 | **No spawn of a test suite inherits `NODE_TEST_CONTEXT`** (claude-dotfiles issue 395). `node --test` exports that variable into every process it spawns, and a nested `node --test` that inherits it EXITS 0 even when a test throws — reproduced on Node 22.22.2: `node --test boom.test.js` exits 1, `NODE_TEST_CONTEXT=child-v8 node --test boom.test.js` exits 0 on the identical failing file. So every place that runs a repo's test command from inside a node test reports green whatever the suite did: `build-dashboard.js` (the dashboard summary spawns the suite, and the summary is exercised from tests) and the session-check engine (`check.js` runs the suite and is itself spawned by `check.test.js`) both did. The rule: a spawn site that may run `node --test`, or a script that runs it, deletes `NODE_TEST_CONTEXT` from the child environment. `templates/build-dashboard.js` gains a `CHILD_ENV` constant (`process.env` minus the variable) and passes it from `sh()`; `templates/pre-commit` gains `unset NODE_TEST_CONTEXT` beside the hook's existing `unset GIT_DIR ...` line (issue 28), for the same defence-in-depth reason. The engine is not per-repo — it ships once at `~/.claude/skills/session-check/check.js` via a `claude-dotfiles` sync pull, so no repo copies it. **Applied in claude-dotfiles** (`scripts/build-dashboard.js`, `.githooks/pre-commit`, the `session-check` engine, regression test `tests/node-test-context.test.js`); the sweep is still owed on the other harnessed repos — aac-bill-intake, aac-sales-commissions, aac-routines, zoho-source-of-truth, aac-contract-builder, aac-message-board, aac-sales-cockpit (aac-routines already fixed its own `tools/run-node-tests.js` under its issue 299). **Both files change, patch surgically** — repo-local `CONFIG` blocks and substituted test commands make a wholesale re-copy destructive (same hazard as v14 and v16) | step 3 (patch `scripts/build-dashboard.js`: add the `CHILD_ENV` constant above `sh()` and pass `env: CHILD_ENV`) + step 5 (patch `.githooks/pre-commit`: add `unset NODE_TEST_CONTEXT`) + step 9 (bump the marker) |
| 24 | 2026-09-16 | The pre-commit gate scrubs git's hook environment completely (claude-dotfiles issue 406). The template already unset `GIT_DIR GIT_INDEX_FILE GIT_WORK_TREE GIT_COMMON_DIR GIT_OBJECT_DIRECTORY` (issue 28); it now also unsets **`GIT_PREFIX`** and carries the incident that shows why the line is load-bearing. On **2026-09-16 in aac-routines** one BLOCKED commit, made from a linked worktree where `GIT_DIR` is `.git/worktrees/<name>`, ran the hook and the suite's own `git init` / `git config` in a temp directory acted on the REAL repository: the main checkout's `.git/config` came out carrying `core.bare = true` and a test identity in `[user]`, and every worktree answered `fatal: this operation must be run in a work tree` until it was repaired by hand. **aac-routines already carries the line**, fixed locally in aac-routines#278, and `claude-dotfiles`' own `.githooks/pre-commit` is patched in this change; every other harnessed repo's hook is stale, so the sweep is the whole delivery. `tests/pre-commit-git-scrub.test.js` fails if a later edit drops any of the five names from either hook. **Patch, do NOT re-copy** — an installed `.githooks/pre-commit` carries that repo's substituted test command, which a wholesale copy destroys | step 5 (patch `.githooks/pre-commit` in place: replace the `unset` line with `unset GIT_DIR GIT_WORK_TREE GIT_INDEX_FILE GIT_PREFIX GIT_COMMON_DIR GIT_OBJECT_DIRECTORY` and add the 2026-09-16 comment above it; a hook with no `unset` line at all takes the whole block from the template. Sweep every harnessed repo — `grep -L GIT_PREFIX */.githooks/pre-commit` names the ones still stale) |
| 25 | 2026-09-16 | `tracker-audit.js`'s `## Blocked by` section ends at its CLAIM, not at end-of-body (claude-dotfiles issue 364, commit `cbee542`). `## Blocked by` is the last heading on many agent-filed tickets, so `proseBlockers()` matching to end-of-body swept whatever followed it into the section — a provenance footer, an attribution line, the harness's own `_Generated by Claude Code_` block — and any `#N` those cited read as a blocker, so the ticket went red with `ungated-dependency` over its own footer. The section now yields the first blank-line-separated block plus any later block that is itself a list; the first non-list block after the claim ends it, and within the claim a list wins over stray lines (once any line is a list item, only list items and their indented continuations are read), so a footer glued to the last bullet with no blank line between them cannot add a blocker either. The `None` short-circuit is judged on the claim alone, so a footer can neither add a blocker nor zero a real one. The template has been GENERATED from this repo's `tools/tracker-audit.js` since v20, so the fix reached the template the moment it landed — but no marker was bumped, so the eight harnessed repos kept running the pre-fix audit with nothing telling them they were behind. **This row is that signal, backfilled (claude-dotfiles issue 434)** — a repo at v24 or lower still holds the pre-fix copy. **Same step, newer file** | step 8 (re-copy `tracker-audit.js` on template-identical repos; on repos with local extensions port `proseBlockers()` and its callers surgically — diff first, same rule as v12) |
| 26 | 2026-09-16 | `templates/dashboard.yml` listens to **two** issue types, `[opened, closed]`, instead of the eight-type list (claude-dotfiles issue 453, carrying out the 2026-09-16 Actions-quota ruling recorded in `docs/tickets/117-decision.md`). Measured over 2026-09-09 -> 2026-09-16: `Refresh dashboard` ran **179.2 runs/day** across the six repos whose issue events were restored that week, **3.09x** the 58 runs/day August 2026 baseline; drop the noisiest repo entirely and the other five still run 1.23x over. `edited`, `labeled`, `unlabeled`, `assigned` and `unassigned` fire on every triage touch — a label flip, a body edit — while the stage-and-diff guard (v6) means most of those runs commit nothing. `opened` and `closed` are the two events that move what the dashboard shows. The trim carries an inline comment naming the ruling, so the next reader does not restore the long list by reflex; a one-off refresh after a label sweep is a `workflow_dispatch` away. **Precedent for the shape of this row:** v14's 2026-09-09 cron footnote. **Patch, do NOT re-copy** — an installed `dashboard.yml` carries the repo's default-branch substitution and its per-repo test-environment setup, which a wholesale copy destroys (same hazard as v6, v14 and v15). **Two workflows this row does not reach:** claude-dotfiles' own `dashboard.yml`, whose `[labeled, unlabeled]` triggers replaced a deleted cron under issue 21, and any repo that replaced issue events with a cron under the 2026-08-10 ruling — read which from each `dashboard.yml` header, never from here | step 4 (patch `.github/workflows/dashboard.yml`: replace the `issues.types` list with `[opened, closed]` and add the comment above it; leave the rest of the file alone) + step 9 (bump the marker) |
| 27 | 2026-09-17 | **The cloud bootstrap hook and the auto-mode posture reach any repo** (claude-dotfiles issue 218, spec #207). Two additions, both delivered by step 16's one command. (a) **The bootstrap hook** — `templates/session-start.sh`, new in this version, is copied to `<repo>/.claude/hooks/session-start.sh` and wired as the first `hooks.SessionStart` entry. It is the ONE per-repo artefact of the cloud cut-over: in a container (`CLAUDE_CODE_REMOTE=true`) it shallow-clones dotfiles master, installs the pinned gh onto PATH through `$CLAUDE_ENV_FILE`, copies the `aac-skills` payload into `~/.claude/skills/`, merges the payload's hooks manifest into the container's user settings so governance fires on prompt 1, writes the marker session-check STOPs on, and emits one sub-2KB `additionalContext` line; a local session exits 0 at once. Before this row a cloud session on any repo but `claude-dotfiles` had no rules text, no governance hooks, no skills and no gh — the marketplace route delivers none of it in cloud, and the two alternatives are both ruled out on timing: the cloud environment setup script runs before the session's git credentials exist, and the plugin's own SessionStart hook cannot install the plugin that has to exist for it to run. The template is a BYTE-IDENTICAL copy of the `.claude/hooks/session-start.sh` that `claude-dotfiles` itself runs, generated by `tools/build-harness-bootstrap-hook.js` there (the issue 336 rule: a second hand-maintained copy is a copy nobody diffs, so it is derived) — identity and not a banner, so re-running the step in `claude-dotfiles` rewrites nothing. (b) **The auto-mode posture** — `permissions.defaultMode: auto`, the blanket allow list and the widened `autoMode.allow` ruling (Dan, 2026-09-15, issue 245) that v19 put in the template while nothing carried it into a repo: v19's row named issue 218 as its delivery, and this is that delivery. `templates/add-cloud-plugin.js` now performs both, detecting an already-wired hook by COMMAND PATH rather than by a tag, so `claude-dotfiles`' own untagged entry gains no duplicate (the issue 166 double-SessionStart shape). Idempotence is the acceptance criterion, not a nicety: a second run prints `already delivered` and leaves `git status` clean. A repo still at v25 or lower has no bootstrap hook, and `/session-start`'s `harness vN is behind v27` STOP (issue 139) is the only thing that says so | step 16 (`node templates/add-cloud-plugin.js <repo>` on every harnessed repo — one pass installs the hook, wires the SessionStart entry and merges the posture; then bump the marker. Verify with a second run printing `already delivered`, and with one cloud session on the repo quoting the `AAC-BOOTSTRAP MARKER` line, `gh --version`, a `gh api repos/{owner}/{repo}` call and session-check's `Cloud bootstrap` block) |
| 28 | 2026-09-17 | **The bootstrap hook fails loudly** (claude-dotfiles issue 483). `templates/session-start.sh` now (a) installs gh BEFORE the dotfiles clone, since the gh tarball is a public download that passes the proxy even when credential injection is broken; (b) on a clone or payload failure writes a FAILED marker (`{"failed": true, "stage", "reason"}`) at `~/.claude/hook-state/aac-bootstrap/state.json` instead of dying under `set -e` with nothing written, prints `aac-bootstrap: STOP - <stage> failed: <cause>` on stderr and exits 0 with one `AAC-BOOTSTRAP STOP:` additionalContext line naming the cause and the fallbacks (MCP `push_files`, `env \| grep ANTHROPIC_BASE_URL`), so session-check's `Cloud bootstrap` block STOPs on the cause rather than on "marker absent". Two sessions on 2026-09-17 read a missing marker as a platform outage when the cause was a caveman proxy URL in the environment (issue 519). Before this row a container with no github.com credential had no marker, no gh and no line saying why | step 16 (`node templates/add-cloud-plugin.js <repo>` rewrites the hook; then bump the marker. Verify with `tests/bootstrap-test.sh --scenario clone-failure` in claude-dotfiles, and in the repo with one cloud session whose session-check `Cloud bootstrap` block reads either `ok aac-bootstrap payload v…` or `STOP aac-bootstrap clone failed — …`) |
| 29 | 2026-09-17 | **The auto-mode rule covers attended sessions and names every category seen** (claude-dotfiles issue 543, widening the issue 245 ruling v19 delivered). Two gaps let the classifier keep refusing. (a) **Scope** — the old text sanctioned "an unattended cloud session or Routine master (no human at the keyboard …)", so a cloud session with its owner watching read as outside the rule; an attended session on 2026-09-17 was refused five times (`[Merge Without Review]` on a read-only `gh api … --jq` that merely shared a turn with an MCP `merge_pull_request`, `[Merge Without Review]` on a `send_later` whose MESSAGE TEXT said to land what is green, `[Irreversible Deletion (general)]` on `rm -rf` of two empty directories a runaway heredoc made at the container root, and `[Modify Shared Resources]` + `[Interfere With Workloads]` on `git merge origin/master` inside a fleet worker's own worktree). The rule now names every cloud session and Routine master on the repo, attended or unattended, and every worker or child session it spawns. (b) **Category names** — the old text enumerated seven and trusted "any successor category" to reach the rest; it did not. All of Merge Without Review, Irreversible Deletion, Modify Shared Resources, Interfere With Workloads, Instruction Poisoning, Create Unsafe Agents, Auto-Mode Bypass and Security Weaken are now named with the shapes that raised them. Three readings are settled in the text: an MCP call (`mcp__github__*`, `mcp__Claude_Code_Remote__*`) is sanctioned exactly as the shell command it replaces, the TEXT of a scheduling or hand-off message is not itself an action, and a read-only command is judged on its own rather than by the most severe command in its turn. `templates/add-cloud-plugin.js` no longer repeats the rule: it READS `autoMode.allow[0]` out of `templates/claude-settings.json` beside it, so what a repo receives and what the template documents cannot drift (the issue 336 rule). A repo at v28 or lower carries the narrow 245 text; the installer prepends the new entry and leaves the old one in place as elaboration | step 16 (`node templates/add-cloud-plugin.js <repo>` on every harnessed repo, then bump the marker. Verify with a second run printing `already delivered`, and with `node --test tools/automode-allow-categories.test.js` in claude-dotfiles) |
| 30 | 2026-09-19 | **The cloud bootstrap hook runs, and the governance hooks it merges execute** (claude-dotfiles issue 614; found doing #600's check on aac-contract-builder). Two defects had made v27-v29 a no-op in every cloud session while every gate stayed green. (a) **Mode bit.** `add-cloud-plugin.js` set the executable bit with `fs.chmodSync`, which git ignores under `core.fileMode=false` — every Windows checkout — so the 2026-09-18 sweep committed `.claude/hooks/session-start.sh` as 100644 and the wired bare-path entry died with exit 126 five milliseconds into SessionStart, before the v28 failure marker could exist: every check read "marker absent", which issues 483 and 519 had documented as two *other* causes. The entry is now `bash "$CLAUDE_PROJECT_DIR/.claude/hooks/session-start.sh"` (quoted; one repo lives under a path with a space), so the bit no longer decides anything, and the installer stages the file 100755 with `git update-index --chmod=+x` when the repo is a git work tree whose index does not already say so. A v27-v29 entry is rewritten in place, once. (b) **`${CLAUDE_PLUGIN_ROOT}`.** Step 4 copied the payload's `hooks.json` commands verbatim into `~/.claude/settings.json`, and Claude Code resolves that variable only for hooks a plugin registered: 16 of 17 merged entries failed on every event (`Hook command references ${CLAUDE_PLUGIN_ROOT} but the hook is not associated with a plugin`, 114 times in one session's log) while the one plain echo printed `AAC-SKILLS HOOK MARKER: plugin hooks execute on this surface`. Each merged command is now seated for settings.json: the token becomes the payload's absolute path under `~/.aac-dotfiles`, prefixed `CLAUDE_PLUGIN_ROOT=<path> PLUGIN_HOOK_GUARD_DISABLE=1` for the scripts that read the variable and for the payload's dedup guard, which would otherwise exit silently because settings.json now names the script. Entries are recognised by that path too, since `claude plugin` commands strip the `_source` tags. The bootstrap gate gained check 7, which EXECUTES every merged SessionStart and UserPromptSubmit command from the empty home (a gate that only reads entries passed three versions of this), and `--fault verbatim-plugin-root` proves it can fail. The clone-failure STOP line now carries the self-heal (call `add_repo` for claude-dotfiles, re-run the hook by its printed path) because the session git proxy ignores credentials in a clone URL and the durable fix — claude-dotfiles as a second source of the environment — is an owner UI step. Also measured, for the record: `extraKnownMarketplaces` never registers the marketplace in cloud (anthropics/claude-code#88248), so the v17/v18 `enabledPlugins` route installs nothing; `claude plugin marketplace add <local clone>` + `claude plugin install` do work there, but only for the next session start | step 16 (`node templates/add-cloud-plugin.js <repo>` on every harnessed repo: rewrites the hook, rewires the entry to the `bash` form and stages the hook 100755 — **commit the mode change**, `git ls-files -s .claude/hooks/session-start.sh` must read `100755`; then bump the marker. Verify with a second run printing `already delivered`, and with one cloud session on the repo whose diagnostics log shows the SessionStart `hook_spawn_completed` with `exit_code: 0`, quoting the `AAC-BOOTSTRAP MARKER` line, and whose `claude-code.log` has no `CLAUDE_PLUGIN_ROOT` refusal after a few prompts) |
| 31 | 2026-09-21 | **The bootstrap no longer depends on which checkout is the project directory** (claude-dotfiles issue 643). The repo-anchored entry `bash "$CLAUDE_PROJECT_DIR/.claude/hooks/session-start.sh"` runs only when the session's project dir IS that repo. A Routine-fired session on 2026-09-21 carried two repository sources, reached neither entry, and ran on what its container image held: a `failed: true` clone marker from 2026-09-19, no skills, no governance hooks, no rules text - and said nothing, because the bootstrap that would have written a fresh marker never ran. Since a cloud environment's home is snapshotted, that state was served to every firing for two days. Step 4 of the hook now also copies itself to `$HOME/.claude/hooks/aac-bootstrap.sh` and seats it as a SessionStart entry in USER settings, which every session in the container runs whatever its project dir is: one successful bootstrap makes every later session in that environment retry. A `mkdir` lock makes the two entries firing together one run, and a healthy marker under 120 seconds old makes a sequential second run a silent no-op. `session-check` gained the matching read: a healthy marker written before this container booted now warns that the hook did not run in this session, instead of reporting the image's payload as this session's | step 16 (`node templates/add-cloud-plugin.js <repo>` on every harnessed repo, then bump the marker; the seat appears in `~/.claude/settings.json` on the next cloud session, and `ls -l ~/.claude/hooks/aac-bootstrap.sh` in that session is the proof) |
