{
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "echo '{\"hookSpecificOutput\": {\"hookEventName\": \"SessionStart\", \"additionalContext\": \"AAC-SKILLS HOOK MARKER: plugin hooks execute on this surface. Quote this sentence verbatim if asked whether the marker is present.\"}}'",
            "timeout": 5
          },
          {
            "type": "command",
            "command": "sh -c 'echo \"AAC-SKILLS RUNTIME PROBE: os=$(uname -s 2>/dev/null || echo unknown) python3=$(command -v python3 || echo none) node=$(command -v node || echo none) pwsh=$(command -v pwsh || echo none) home=$HOME\"'",
            "timeout": 5
          }
        ]
      }
    ]
  }
}
